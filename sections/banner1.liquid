<section class="main-banner">
<div class="banner__content" >
    </div>

</section>

    <style>
        .main-banner{
        background-image:url('{{ section.settings.ip | image_url: "1920x1080" }}');
        background-size: cover;
        background-position: center;
        text-align: left;
        padding: 80px;
        color: black;
        }
        .banner__content {
            max-width: 600px;
          }
        
        </style>

        {% schema %}
        {
        "name": "banner1",
        "tag": "section",
        "limit": 2,
        "class": "banner1",
        "settings": [

            {
            "type": "image_picker",
        "id": "ip",
             "label": "image-pick"
           
    },
            {
            "type": "text",
            "id": "title",
            "label": "title",
            "default":"test text"
            },
            
        ],
        "presets": [
            {
            "name": "banner1",
        }
        ]
        
        }


        {% endschema %}